language: node_js

node_js:
 - 4.4.3

script:
  - mocha

after_success:
  - if [ $BRANCH == "develop" ]; then git push -f git@heroku.com:beta-simian.git $BRANCH:master; fi
  - if [ $BRANCH == "master" ]; then git push -f git@heroku.com:simian-website.git $BRANCH:master; fi

# include
branches:
  only:
    - master
    - develop

cache: true
